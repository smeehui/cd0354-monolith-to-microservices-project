apiVersion: v1
kind: Service
metadata:
  name: udagram-api-user
  labels:
    run: udagram-api-user
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: udagram-api-user